apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: ai-platform
  labels:
    app: vllm
spec:
  selector:
    app: vllm
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
