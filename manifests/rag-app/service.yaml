apiVersion: v1
kind: Service
metadata:
  name: rag-app
  namespace: ai-platform
  labels:
    app: rag-app
spec:
  selector:
    app: rag-app
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
